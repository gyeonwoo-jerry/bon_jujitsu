{
  "name": "bonbjj",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.8.3",
    "bonbjj": "file:",
    "cors": "^2.8.5",
    "cra-template": "1.2.0",
    "http-proxy-middleware": "^3.0.3",
    "mysql": "^2.18.1",
    "nth-check": "^2.1.1",
    "postcss": "^8.5.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^6.22.3",
    "react-scripts": "^5.0.1",
    "web-vitals": "^4.2.4",
    "webpack": "^5.98.0"
  },
  "scripts": {
    "start": "export NODE_OPTIONS=--openssl-legacy-provider && react-scripts start",
    "build:dev": "export NODE_ENV=development NODE_OPTIONS=--openssl-legacy-provider && react-scripts build",
    "build:prod": "export NODE_ENV=production NODE_OPTIONS=--openssl-legacy-provider && react-scripts build",
    "test": "export NODE_OPTIONS=--openssl-legacy-provider && react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
